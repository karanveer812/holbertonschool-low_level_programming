#include "main.h"
#include <stdio.h>

/**
 * add - add two integers
 * @a: takes integers as an input
 * @b: takes integers as an input
 *
 * Return: result
 */
int add(int a, int b)
{
	return ((a + b));
}
