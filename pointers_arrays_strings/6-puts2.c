#include "main.h"

/**
 * rev_string - output the string in reverse
 * @s: takes string as input
 */
void puts2(char *str)
{
	char x;

	while (str[n] != '\0')
	{
		n++;
		if (n % 2 == 0)
		{
			_putchar(str[n]);
		}
	}
}
