#include "main.h"
#include <stdio.h>

/**
 * puts_half - Print the second half of a string
 * @str: The input string
 */
char *_strcat(char *dest, char *src)
{
	puts(dest);
	puts(src);

	return ("test");
}
